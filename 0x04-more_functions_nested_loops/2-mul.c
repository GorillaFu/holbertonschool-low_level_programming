#include "holberton.h"
/**
 * mul - return de value of multiplication between a and b
 * @a: input one
 * @b: input two
 * Return: result of multiplication
 */
int mul(int a, int b)
{
	int  mult;

	mult = a * b;

	return (mult);
}
