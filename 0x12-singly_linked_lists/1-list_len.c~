#include <stdlib.h>
#include <string.h>
#include <stdio.h>
#include "lists.h"

/**
 * list_len - returns number of nodes in list
 * @h: pointer to list
 * Return: Always 0.
 */
size_t list_len(const list_t *h)
{
	int counter = 0;

	while (h)
	{
		if(h
	}
}
